//wap to accept two integers and check whether they are equal or not.

#include<stdio.h>
void main()
{
	int m,n;
	printf("enter two nos.: ");
	scanf("%d%d",&m,&n);
	if(m==n)
	{
	printf("both the integers are equal");	
	}
	else
	{
	printf("both the integers are not equal");
	}
}